import { FiltrareNewsPipe } from './filtrare-news.pipe';

describe('FiltrareNewsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrareNewsPipe();
    expect(pipe).toBeTruthy();
  });
});
